apiVersion: apps/v1
kind: Deployment
metadata:
  name: flightnetwork_automation
spec:
  selector:
    matchLabels:
      app: flightnetwork_automation
  template:
    metadata:
      labels:
        app: flightnetwork_automation
    spec:
      containers:
      - name: myapp
        image: <Image>
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: <Port>
